{
  "private": true,
  "workspaces": {
    "packages": [
      "account",
      "checkout",
      "helpinfo",
      "shell",
      "provider-lib"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "serve": "lerna run --parallel serve",
    "build-helpers": "lerna run build --scope capacitor-portals --scope provider-lib",
    "bootstrap": "lerna bootstrap",
    "build": "export CAP_BUILD=true; lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "name": "capacitor-portals-ecommerce"
}
